import 'package:flutter/material.dart';

class ProfileTherapist extends StatelessWidget {
  const ProfileTherapist({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(),
    );
  }
}
